{
  "name": "@tabletalk/backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "env": "bunx convex env set --from-file .env.local --force && bunx convex env list",
    "generate-auth-schema": "cd convex/betterAuth && bunx auth generate"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "convex": "^1.42.1",
    "convex-helpers": "^0.1.120",
    "dotenv": "^17.4.2",
    "openai": "^6.49.0",
    "uuid": "^14.0.1",
    "zod": "^4.4.3"
  }
}
